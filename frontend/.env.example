# Backend API base URL
# Leave empty (or omit) when frontend and backend share the same domain/proxy
# Set to the full backend URL when deploying separately
# VITE_API_URL=https://api.yourdomain.com/api
